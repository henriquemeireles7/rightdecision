{
  "scripts": {
    "setup": "bun install && [ -f .env ] || cp .env.example .env && sed -i '' \"s/PORT=3000/PORT=$CONDUCTOR_PORT/\" .env && sed -i '' \"s|localhost:3000|localhost:$CONDUCTOR_PORT|\" .env",
    "run": "PORT=$CONDUCTOR_PORT bun run dev"
  },
  "runScriptMode": "concurrent"
}
